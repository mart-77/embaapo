<window viewModel="@id('vm') @init('viewmodel.CalificacionViewModel')" title="Formulario de Calificación" border="normal">
    <vbox>
        <label value="Nombre del Seller:" />
        <combobox model="@load(vm.nombresSellers)" selectedItem="@bind(vm.nombreSeleccionado)" />

        <label value="Descripción:" />
        <textbox value="@bind(vm.descripcion)" />

        <label value="Puntuación:" />
        <intbox value="@bind(vm.puntuacion)" />

        <button label="Guardar Calificación" onClick="@command('guardarCalificacion')" />
        
        <!-- Espacio para mensajes -->
        <label value="@load(vm.mensaje)" style="color: red;" />
    </vbox>
</window>
